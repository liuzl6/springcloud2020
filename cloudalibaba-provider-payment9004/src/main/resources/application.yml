server:
  port: 9004

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: nacos.com:80    #配置Nacos注册中心地址  集群版

#暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: '*'